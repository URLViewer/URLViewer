# タスク

- [x] `UrlViewer-Plugins/url-extractor-input-plugin/src/index.ts` のエラー原因を特定する。
- [x] `SidToM3u8` 互換の方針（`index.ts` 単体で型エラーを出さない構成）で修正する。
- [x] 型チェックで `index.ts` のエラーが解消されたことを確認する。
- [x] ビルド（JS）に回帰がないことを確認する。
