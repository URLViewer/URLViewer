# 実装計画

1. ライブラリヘッダーを2段構成に分離し、`ライブラリ` タイトルと `選択` ボタンを1段目、ソートUIを2段目に配置する。
2. ソートチップに `whitespace-nowrap` を付与して `追加順` などの文字折返しを防止する。
3. 動画アイテム右側アクションボタンを小型化して、ラベル入力欄の可視幅を確保する。
4. `yarn typecheck` を実行して整合性を確認する。
