# 実装計画

1. `GroupPanel` のグループ並び替えを favorites 優先 + 名前順に変更する。
2. `GroupPanel` のグループタイトル描画で favorites のときだけ星アイコンを先頭表示する。
3. `VideoPlayer` の `displayedGroups` 並び替えを favorites 優先に変更する。
4. 「グループに追加」リスト項目に favorites 用クラスを追加し、軽い強調スタイルを当てる。
5. `yarn typecheck` を実行する。
