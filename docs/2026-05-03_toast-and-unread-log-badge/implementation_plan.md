# 実装計画

1. `App.tsx` でイベントログの追加を監視し、最新ログが `success/error` の場合にトーストを表示する。
2. トーストは自動消滅（約3.2秒）と手動クローズを両方サポートする。
3. `App.tsx` で未読ログ件数を状態管理し、ログパネル未表示時に新規ログで加算する。
4. ログパネルを開いたタイミングで未読件数を 0 に戻す。
5. ログボタンにバッジを表示し、件数が 99 を超えた場合は `99+` 表示にする。
6. `index.css` にトースト・未読バッジのスタイルを追加する。
7. `yarn typecheck` で検証する。
