# 実装計画

1. `LibrarySortKey` に `added` を追加し、並び替えロジックに追加順を実装する。
2. ライブラリヘッダーの並び替えUIをピル型セグメントに変更し、順序反転ボタンを統一感のある見た目にする。
3. 動画追加フロー（通常入力/プラグイン入力/検証実行後）で、新規追加動画の `durationSeconds` を `loadedmetadata` から取得し保存する。
4. 動画ロックのトグルヘルパーとストアアクションを追加し、アイテム上のロックボタンを有効化する。
5. 動画アイテムを `item-shell-video` + `video-item-actions` 構成に変更し、ボタン群の折り返し崩れを防ぐ。
6. `yarn typecheck` を実行して型整合性を確認する。
