# タスク

- [x] `plugins:installFromGit` が GitHub の `tree/<branch>/<path>` URL で 404 になる原因を特定する。
- [x] `tree` URL を clone 可能なリポジトリURLへ正規化する。
- [x] `tree` URL のサブディレクトリを plugin root として導入できるようにする。
- [x] 型チェックで修正の整合性を確認する。
